# 案例对话：整理“CPU GPU NPU”原始笔记

下面直接展示“用户输入什么，Agent 怎样回应”。内容依据 Vault 中现存的三篇 Concept Note、一张 Topic Map 和六组图片附件精简复原，不是原聊天逐字稿。

原始文件 `00-原始笔记/CPU GPU NPU.md` 当前已不在输入目录，因此无法还原原文的全部行号、覆盖账本和历史确认原话。文中标为“当前规则下应有”的输出，是用现在的 `integrate-learning` 规则补齐的规范示范，不冒充历史事实。

## 1. 用户指定原始笔记

**用户**

> `@"00-原始笔记/CPU GPU NPU.md"` 帮我把这篇笔记整理进知识库中。

**Agent**

> 我会调用 `integrate-learning`，只处理：
>
> ```text
> 00-原始笔记/CPU GPU NPU.md
> ```
>
> 不会自动扫描其他原始笔记。这个流程只忠实转换指定文件，不补写原文之外的新知识，也不等同于 Teach 或掌握评估。
>
> 我先冻结输入清单并检查所有图片的下一行说明，然后再提交整合计划；现在不会直接写入正式知识。

## 2. Agent 预检图片

**Agent**（根据正式产物中保留的图片对精简复原）

> 检查到 6 组图片—说明对：
>
> ```text
> 1. CPU运行3D.png
>    （CPU只能一行行去计算点的坐标，所以很耗时）
>
> 2. Pasted image 20260830235904.png
>    （这些计算单元同时工作，同时计算出结果）
>
> 3. Pasted image 20260831000241.png
>    （CPU可以跳转到需要的代码去执行，但是GPU只能完成所有计算采取赋值策略）
>
> 4. Pasted image 20260831000555.png
>    （CPU可以直接跳转到b=a+3运算，但是GPU要执行全部的运算，包括不符合条件的，然后又由于这串代码每次计算都依赖前面的计算结果，无法体现并行计算的优势）
>
> 5. Pasted image 20260831000903.png
>    （现代GPU架构，RT Core为光线追踪，CUDA Core负责图形渲染和通用科学计算）
>
> 6. Pasted image 20260831001330.png
>    （不同搭载NPU的云边端设备）
> ```
>
> 这些说明能够告诉我图片在笔记中的知识作用。我只依据括号文字处理，不声称已经直接识别图片像素。

如果有图片没有说明，当前规则下 Agent 应这样回应：

**Agent**（缺少说明时）

> 以下图片的下一物理行没有合格的括号说明：
>
> ```text
> <笔记路径>:<行号>  <图片目标>  — 缺少说明/说明不足
> ```
>
> 请先在原笔记中补充。其他不依赖图片的正文可以继续分析，但我不能宣布 100% 覆盖、不能晋升依赖图片的知识，也不能清理输入。

## 3. Agent 展示拆分计划

**Agent**（依据实际输出还原）

> 这篇原始笔记包含三个边界不同的核心命题，不适合整体压成一篇“CPU GPU NPU”大笔记。我建议拆成：
>
> | 目标文件 | 核心命题 | 主要材料 |
> | --- | --- | --- |
> | `02-概念/CPU 与 GPU 的执行方式：控制流与并行.md` | CPU 偏向控制流，GPU 适合大量独立、重复的并行计算；分支和串行依赖会削弱并行优势 | 直线插值例子、CPU/GPU 计算图、分支示意 |
> | `02-概念/CPU、GPU 与 NPU 的架构对比.md` | 三类处理器因设计目标不同，在核心结构、并行、延迟、功耗、任务和算力口径上分化 | CPU/GPU/NPU 对比和 SG2002 分工案例 |
> | `02-概念/神经网络加速：Tensor Core 与 NPU 的 Cube Core.md` | 神经网络矩阵计算推动 GPU 专用矩阵核和 NPU 专用单元，并用于云边端部署 | GPU 核心分类图、Cube Core、云边端设备图 |
>
> 再创建一张主题地图：
>
> ```text
> 03-主题地图/处理器与加速器.md
> ```
>
> 地图按语义组织，而不是平铺文件名：
>
> ```text
> 定位与对比
> └── CPU、GPU 与 NPU 的架构对比
>
> 通用执行方式
> └── CPU 与 GPU 的执行方式：控制流与并行
>
> 专用矩阵加速
> └── 神经网络加速：Tensor Core 与 NPU 的 Cube Core
> ```
>
> 图片去向：
>
> - 4 张 CPU/GPU 执行与分支图片进入“CPU 与 GPU 的执行方式”；
> - GPU 架构图和云边端设备图进入“神经网络加速”；
> - 每张图片与下一行括号说明保持在一起。
>
> 关系计划：
>
> - 三篇 Concept Note 都链接“处理器与加速器”；
> - 架构对比分别细化到通用执行方式和专用矩阵加速；
> - 成立的关系在两侧都显式写入。
>
> 标签计划：
>
> - 普通概念使用宽泛领域标签；
> - Topic Map 的标签只使用 `map`。
>
> 我只会结构化改写原笔记已经提供的内容，不添加新的定义、机制、数值、案例或结论。是否按这个批次计划执行？

## 4. 用户一次确认整批写入

**用户**

> 可以，按这个计划执行。

**Agent**

> 已获得这份计划中非破坏性创建和更新的一次性授权。我会在写入前重新读取目标文件，避免覆盖确认后出现的新修改；如果目标或内容范围发生实质变化，会暂停并重新展示偏差。

## 5. Agent 写入第一篇：CPU 与 GPU 的执行方式

**Agent**（展示精简片段）

> 已创建：
>
> ```text
> 02-概念/CPU 与 GPU 的执行方式：控制流与并行.md
> ```
>
> 其中把直线插值例子用于说明“多个数据点执行相同计算”：
>
> ```text
> α = (9 - 3) / (16 - 4) = 0.5
> b = 3 - 0.5 × 4 = 1
> y = 0.5x + 1
> ```
>
> 图片与说明仍然紧邻：
>
> ```markdown
> ![[CPU运行3D.png]]
> （CPU只能一行行去计算点的坐标，所以很耗时）
>
> ![[Pasted image 20260830235904.png]]
> （这些计算单元同时工作，同时计算出结果）
> ```
>
> 分支和串行依赖图片放在“控制流”机制附近，没有集中堆到附件区。

## 6. Agent 写入架构对比与专用加速笔记

**Agent**（展示精简结果）

> 已创建：
>
> ```text
> 02-概念/CPU、GPU 与 NPU 的架构对比.md
> ```
>
> 这篇只承担三类处理器的定位和对比，保留原材料中的设计目标、核心结构、并行、延迟、功耗、擅长任务、算力口径和 SG2002 分工案例。
>
> 已创建：
>
> ```text
> 02-概念/神经网络加速：Tensor Core 与 NPU 的 Cube Core.md
> ```
>
> 这篇只展开神经网络矩阵加速和云边端部署，并保留：
>
> ```markdown
> ![[Pasted image 20260831000903.png]]
> （现代GPU架构，RT Core为光线追踪，CUDA Core负责图形渲染和通用科学计算）
>
> ![[Pasted image 20260831001330.png]]
> （不同搭载NPU的云边端设备）
> ```

## 7. Agent 写入 Topic Map 和双向关系

**Agent**

> 已创建：
>
> ```text
> 03-主题地图/处理器与加速器.md
> ```
>
> 当前结构：
>
> ```text
> 定位与对比
> - [[CPU、GPU 与 NPU 的架构对比]]
>
> 通用执行方式
> - [[CPU 与 GPU 的执行方式：控制流与并行]]
>
> 专用矩阵加速
> - [[神经网络加速：Tensor Core 与 NPU 的 Cube Core]]
> ```
>
> 三篇 Concept Note 都反向链接地图。架构对比笔记还分别链接两篇细化笔记；相关笔记之间写明关系类型和理由，不只依赖 Obsidian 的自动反向链接面板。

## 8. Agent 回读并报告覆盖

由于历史覆盖账本已经不可读取，下面展示当前规则下完成写入后应给出的精简回执，而不是伪造当时的逐项数量。

**Agent**（当前规则下应有）

> 回读验证完成：
>
> - 3 篇 Concept Note 和 1 张 Topic Map 均存在；
> - 每篇笔记只有一个核心命题；
> - 6 组图片与括号说明保持物理相邻，附件目标可以解析；
> - Topic Map 使用 `map` 标签并按语义分组；
> - Concept Note 与地图、概念之间的关系两侧均已写入；
> - 没有把计划外的新知识混入输出。
>
> 覆盖报告应逐项使用：
>
> ```text
> 已整合 → <目标文件>
> 已合并 → <目标文件#段落>
> 已保留 → <精确位置>
> 明确丢弃 → <用户确认的理由>
> 未处理 → <原因>
> ```
>
> 只有所有原始知识单元都有最终去向、没有 `未处理`，并且输出中的实质内容都能追溯到输入时，才能报告 100% 覆盖。

## 9. 有可能不准确的原文怎么办

如果原始笔记含有绝对表述、缺少来源的具体性能范围或待确认的厂商术语，`integrate-learning` 不能用模型常识静默改正，也不能因为怀疑就删除。

**Agent**（当前规则下应有）

> 我会按“原始笔记观点 / 待核查”保留相关内容。完成转换后，另外给出：
>
> ```text
> 质疑与待核查
> - 建议核查原文中的绝对表述和具体性能数字。
> - 这些建议未写入正式笔记，不属于本批次转换内容，也不计入覆盖率。
> ```
>
> 如果你选择核查，再单独调用 `verify`；核查结果不能倒灌进已经确认的封闭语料转换。

## 10. Agent 询问是否清理原始输入

当前 Vault 中已经找不到原始 `CPU GPU NPU.md`，现有信息无法证明它当时是怎样离开输入目录的。因此不能把其缺失描述成一次符合当前规则的清理。

当前规则下，只有真正达到 100% 覆盖后才应出现下面的对话：

**Agent**（当前规则下应有）

> 整合和回读已完成。是否把下面这一个 Markdown 可恢复地移入 `.trash/`？
>
> ```text
> 00-原始笔记/CPU GPU NPU.md
> → .trash/00-原始笔记/CPU GPU NPU.md
> ```
>
> 图片附件不会被静默移动或删除；移动前还会验证原笔记中的图片引用不会失效。这不是永久删除。

**用户**

> 可以移入 `.trash/`。

**Agent**

> 已移动并回读验证。永久删除仍需你以后明确调用：
>
> ```text
> /skill:purge-trash
> ```

## 11. 为什么这个流程没有评估用户

**用户**

> 为什么整理完成后没有问我评估题？

**Agent**

> 因为这次调用的是 `integrate-learning`：它只负责把你已经写下的材料忠实整理进知识网络，不代表你刚刚接受了教学，也不能制造新的掌握证据。
>
> 如果你想系统学习并验证是否会用，可以另外开始：
>
> ```text
> /skill:teach 我想理解 CPU、GPU 和 NPU 为什么采用不同的计算结构，并能为实际任务选择处理器
> ```

